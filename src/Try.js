import { useTransition } from '@react-spring/core'
import { animated } from '@react-spring/web'
import React from 'react'
import { useState } from 'react'
import NewHead from './components/Header/NewHead'
import TryHelper from './components/TryHelper'
import cat from '..//src/components/assets/photo/library1.jpg'
const Try = (props) => {



  
  return (
  <div class="login" style={{'backgroundImage' : `url(${cat})`}}>jj</div>
  )
}

export default Try